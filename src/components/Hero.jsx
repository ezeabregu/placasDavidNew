import React from "react";
import { HeroContainer, HeroTitle, HeroSubtitle } from "./heroStyles";

const Hero = () => {
  return (
    <HeroContainer>
      <img src="/assets/logonobackground.png" alt="logo" />
    </HeroContainer>
  );
};

export default Hero;
